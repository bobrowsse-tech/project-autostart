services:
  db:
    image: postgres:16
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD", "pg_isready"]
  api:
    image: example/api
    ports:
      - "3000:3000"
    depends_on:
      - db
  web:
    image: example/web
    ports:
      - "8080:80"
    depends_on:
      - api
